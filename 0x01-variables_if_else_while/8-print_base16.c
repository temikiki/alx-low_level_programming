#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char hi;
	int x;
	for (x = 48; x <= 57; x++)

	{
	putchar(x);
	}
	for (hi = 'a'; hi <= 'f'; hi++)

	{
	putchar(hi);
	}
	putchar('\n');
	return (0);
}
